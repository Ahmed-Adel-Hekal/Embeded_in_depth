#include <stdio.h>
#include <stdlib.h>



int main() {
    const int x = 10;
    int *ptr = &x; // This line will generate a warning
    *ptr = 20;
    printf("The value of x is %d\n", x);
    return 0;
}

